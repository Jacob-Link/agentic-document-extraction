name: agentic-extraction
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    - browser-use
    - fastapi
    - uvicorn
    - boto3
    - python-dotenv
    - google-generativeai
    - playwright
    - requests
    - aiofiles